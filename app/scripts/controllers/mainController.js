define(['underscore', 'marionette', 'App', 'utils/templates', 'utils/appConfig'], function(_, Marionette, App, Templates, AppConfig) {
        'use strict'
        var self;
        return Marionette.Controller.extend({
                initialize: function() {
                    
                },

                home: function(tabId) {

                    require(['c'],function(c)){

                    });
                }        
            });
});
